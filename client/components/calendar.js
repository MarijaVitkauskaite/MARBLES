import { ScrollView, StyleSheet, Text, View } from "react-native";
import moment from "moment";

export default function HorizontalScrolly ({currentDate, onSelectDate, showDaysAfterCurrent, showDaysBeforeCurrent}) {
    
}